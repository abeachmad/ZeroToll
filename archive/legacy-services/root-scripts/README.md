# Legacy Root Wrappers

These are deprecated top-level wrappers that previously launched older relayer,
bundler, or policy-server flows.

They were moved out of the repo root to make the official runtime path clearer.

Prefer:

- `./start-zerotoll.sh`

Only use these for historical debugging of legacy flows.
