Legacy backend entry points live in this folder.

They are kept for reference and debugging, but they are not part of the
official local runtime. Use `./start-zerotoll.sh` from the repo root for the
supported stack.
